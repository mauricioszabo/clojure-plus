(ns clojure-plus.helpers-test
  (:require [clojure-plus.helpers :as helpers]
            [clojure-plus.core :as core]))

(require '[clojure-plus.helpers :as helpers])
(api/all-ns)
(expect 10 20)

helpers/a
; (use 'expectation)

; (t/testing "fooBar"
;   (t/is (= 1 2)))
